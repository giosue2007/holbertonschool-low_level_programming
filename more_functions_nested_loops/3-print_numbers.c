#include "main.h"
#include <stdio.h>
/**
 * print_numbers - check the code.
 *
 * @c: character to be checked
 * Return: Always 0.
 */
void print_numbers(void)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		_putchar
	}
}

int main(void)
{
    print_numbers();
    return (0);
}
