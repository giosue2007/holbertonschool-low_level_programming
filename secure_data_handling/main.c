#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "store.h"

int main(void) {
    /* Ton code de boucle while(1) avec les fgets, add, get, etc. */
    /* Assure-toi que cette fonction s'appelle bien 'main' */
    return (0);
}